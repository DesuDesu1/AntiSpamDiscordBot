apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ .Release.Name }}-gateway
  labels:
    app: gateway
spec:
  replicas: {{ .Values.gateway.replicaCount }}
  selector:
    matchLabels:
      app: gateway
  template:
    metadata:
      labels:
        app: gateway
    spec:
      containers:
        - name: gateway
          image: "{{ .Values.gateway.image.repository }}:{{ .Values.gateway.image.tag }}"
          imagePullPolicy: {{ .Values.gateway.image.pullPolicy }}
          env:
            - name: Discord__Token
              valueFrom:
                secretKeyRef:
                  name: {{ .Release.Name }}-secrets
                  key: discord-token
            - name: Discord__OwnerId
              valueFrom:
                secretKeyRef:
                  name: {{ .Release.Name }}-secrets
                  key: discord-owner-id
            - name: Kafka__BootstrapServers
              valueFrom:
                configMapKeyRef:
                  name: {{ .Release.Name }}-config
                  key: KAFKA_BOOTSTRAP_SERVERS
            - name: Internal__BotBaseUrl
              valueFrom:
                configMapKeyRef:
                  name: {{ .Release.Name }}-config
                  key: BOT_INTERNAL_BASE_URL
            - name: Internal__ApiKey
              valueFrom:
                secretKeyRef:
                  name: {{ .Release.Name }}-secrets
                  key: internal-api-key
          resources:
            {{- toYaml .Values.resources.gateway | nindent 12 }}
